{
  "name": "money-map",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "@playwright/test": "^1.48.0",
    "serve": "^14.2.0"
  },
  "dependencies": {
    "wa-sqlite": "^1.0.0"
  }
}
